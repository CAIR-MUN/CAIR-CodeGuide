title: Home
layout: default

# CAIR Coding Guide

Welcome to the CAIR Coding Guide site. This site is generated from the repository's wiki content and served via GitHub Pages.

Use the navigation links to the left (or the site index) to read the chapters.

If you want to preview locally, install Jekyll (`gem install jekyll bundler`) and run:

```bash
bundle exec jekyll serve --source docs
```
